% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_tree_app.R
\name{mod_tree_app}
\alias{mod_tree_app}
\title{Display a module tree from a running Shiny app object}
\usage{
mod_tree_app(app, app_fun = "app", ui_fun = NULL, server_fun = NULL)
}
\arguments{
\item{app}{A \code{shiny.appobj}, as returned by \code{\link[shiny:shinyApp]{shiny::shinyApp()}}.}

\item{app_fun}{Character scalar; label used for the root node of the tree.
Defaults to \code{"app"}. Because the app object does not store the name of
the launch function, this is a display label only.}

\item{ui_fun}{Character scalar or \code{NULL}; name of the UI function.
When \code{NULL} (the default), \code{mod_tree_app()} attempts to recover the UI
function automatically. Auto-detection succeeds when \code{ui} was passed to
\code{\link[shiny:shinyApp]{shiny::shinyApp()}} as an \strong{unevaluated} function reference
(e.g. \code{shinyApp(ui = app_ui, ...)}). If \code{ui} was pre-evaluated
(e.g. \code{shinyApp(ui = app_ui(), ...)}), supply the function name
explicitly so the UI branch can be included in the tree.}

\item{server_fun}{Character scalar or \code{NULL}; name of the server function.
When \code{NULL} (the default), the name is inferred by comparing function
bodies against those visible in the server's environment.}
}
\value{
Invisibly returns the nested tree structure (a list). The tree is
printed to the console as a side effect.
}
\description{
\code{mod_tree_app()} inspects a \code{shiny.appobj} at runtime rather than parsing
source files on disk. It extracts the server function via
\code{\link[shiny:shinyApp]{shiny::shinyApp()}}'s internal \code{serverFuncSource} slot, collects all
functions visible in the server's enclosing environment, identifies Shiny
modules with \code{\link[=uses_ns]{uses_ns()}}, and renders the same plain-text tree as
\code{\link[=mod_tree]{mod_tree()}}.
}
\examples{
\dontrun{
app <- shiny::shinyApp(ui = app_ui, server = app_server)
mod_tree_app(app)

# pre-evaluated UI requires explicit ui_fun
app2 <- shiny::shinyApp(ui = app_ui(), server = app_server)
mod_tree_app(app2, ui_fun = "app_ui")

# non-default names
mod_tree_app(app, app_fun = "launch", ui_fun = "movies_ui", server_fun = "movies_server")
}

}
\seealso{
\code{\link[=mod_tree]{mod_tree()}} for the file-based equivalent.
}
