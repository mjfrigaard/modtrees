% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_tree.R
\name{mod_tree}
\alias{mod_tree}
\title{Display a Shiny function module tree}
\usage{
mod_tree(
  path = "R",
  app_fun = "launch",
  ui_fun = "app_ui",
  server_fun = "app_server"
)
}
\arguments{
\item{path}{Character scalar; path to the directory containing R source
files. Defaults to \code{"R"}.}

\item{app_fun}{Character scalar; name of the top-level app launch function.
Defaults to \code{"launch"}.}

\item{ui_fun}{Character scalar; name of the UI function.
Defaults to \code{"app_ui"}.}

\item{server_fun}{Character scalar; name of the server function.
Defaults to \code{"app_server"}.}
}
\value{
Invisibly returns the nested tree structure (a list). The tree
is printed to the console as a side effect.
}
\description{
\code{mod_tree()} statically parses all R source files in a directory,
detects Shiny modules (via calls to \code{\link[shiny:NS]{shiny::NS()}} or
\code{\link[shiny:moduleServer]{shiny::moduleServer()}}), builds a call graph, and prints a
plain-text tree showing the module hierarchy.
}
\examples{
\dontrun{
mod_tree("R")
mod_tree("R", app_fun = "run_app", ui_fun = "ui", server_fun = "server")
}

}
